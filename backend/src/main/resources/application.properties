spring.application.name=polyhub
# Tự động load file application-local.properties (gitignored) để đọc secret keys
spring.config.import=optional:classpath:application-local.properties

spring.datasource.url=jdbc:mysql://localhost:3306/polyhub?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://POLYHUB_DB:POLYHUB@polyhub.reph6nz.mongodb.net/polyhub_chat?retryWrites=true&w=majority&appName=POLYHUB}
spring.mongodb.uri=${SPRING_MONGODB_URI:mongodb+srv://POLYHUB_DB:POLYHUB@polyhub.reph6nz.mongodb.net/polyhub_chat?retryWrites=true&w=majority&appName=POLYHUB}

server.port=${PORT:8080}

# Cấu hình giới hạn kích thước file tải lên (Fix lỗi MaxUploadSizeExceededException)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
server.tomcat.max-swallow-size=-1

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dueb9a4d9}
cloudinary.api_key=${CLOUDINARY_API_KEY:355974684552594}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:RMcyJX2hM5TH7YFCUgL2-WR6I40}

# Gemini AI Configuration
# Đặt API key thực vào file .env (đã được gitignore)
# Xem .env.example để biết cách cấu hình
gemini.api.key=${GEMINI_API_KEY:your-gemini-api-key-here}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent}

# Email Sender Configuration
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:nmn00525@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:xksl cywc bxha kvcn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.main.allow-bean-definition-overriding=true

# FPT.AI eKYC Configuration
fpt.ai.api-key=${FPT_AI_API_KEY:bJ2yNEB7cmR9RVlC5qknkWoSTO9zT9GA}
# Groq AI Configuration (fallback khi Gemini hết quota)
groq.api.key=${GROQ_API_KEY:}
groq.api.url=https://api.groq.com/openai/v1/chat/completions
groq.model=llama-3.3-70b-versatile

# Python AI Services Configuration
local.ocr.url=${LOCAL_OCR_URL:http://localhost:8001/ocr-cccd?side=auto}
local.face.url=${LOCAL_FACE_URL:http://localhost:8002/verify-face}

# Enable test bypass for mentor registration
app.mentor-test-bypass.enabled=true
